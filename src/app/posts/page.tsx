import { getAllPosts } from "@/api/post";
import { Post } from "@/components/post";

export default function PostsPage() {
  return (
    <main>
      <h1>Posts</h1>

      {/* TODO: Fetch all posts and list here */}
      {/* <Post post={post} /> */}
    </main>
  );
}
